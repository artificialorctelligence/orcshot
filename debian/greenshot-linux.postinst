#!/bin/sh
set -e

case "$1" in
    configure)
        echo ""
        echo "Greenshot Linux is installed. Open it from your Applications menu"
        echo "(or run: greenshot-linux) to finish setup - it will offer to enable"
        echo "autostart and capture keyboard shortcuts."
        echo ""
        ;;
esac

#DEBHELPER#

exit 0
